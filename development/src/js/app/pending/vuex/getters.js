export const pendingLog = (state) => {
  return state.pending
}

export const pendingAmount = (state) => {
  return state.pendingAmount
}

export const pendingCalculations = (state) => {
  return state.calculations
}
