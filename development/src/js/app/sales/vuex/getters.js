export const salesLog = (state) => {
  return state.sales
}

export const salesAmount = (state) => {
  return state.salesAmount
}

export const salesCalculations = (state) => {
  return state.calculations
}
